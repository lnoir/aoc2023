import { loadLinesFromFile } from "../helpers";

export function parseLine(line: string): Record<string, number> {

  return 
}

export function getSolution(file?: string): number {
  const lines = loadLinesFromFile(file || './02/02.input.txt');

  return
}